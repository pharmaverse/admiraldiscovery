# Presentation Archive

This data set contains information and links to resources of recent
presentations about admiral-related topics.

## Usage

``` r
presentation_archive
```

## Format

An object of class `spec_tbl_df` (inherits from `tbl_df`, `tbl`,
`data.frame`) with 22 rows and 5 columns.
